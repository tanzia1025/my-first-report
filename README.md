# my-first-report

#include <stdio.h>

int main()
{
    int s,l,m;
    printf("Enter the value:\n");
    scanf("%d %d %d",&s,&l,&m);
    float avg=(s+l+m)/3;
    printf("the average if 3 value is :%f",avg);

    return 0;
}
